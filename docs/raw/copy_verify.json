{
 "results": [
  {
   "axis": "code-equivalence",
   "ok": true,
   "confidence": "high",
   "findings": [
    {
     "severity": "note",
     "what": "The 32 numbered output directories are invisible to git, so \"git status is clean\" is NOT evidence the 13 GB of PNGs survived. /home/sjpark/Documents/SCSegmentation/.gitignore contains a bare `*.png` rule (line reads `*.png` with trailing spaces, which git strips), and `git ls-files SCSam3/demoSCSam3OneStageNew/` returns only 14 entries — the 13 .py files plus .dockerignore. Nothing under 0..31 is tracked.",
     "why": "If a future copy/sync had wiped or truncated the numbered directories, git would have reported nothing at all. Their survival can only be confirmed by filesystem inspection — which I did (32/32 dirs, 50 PNGs each, 1600 total, 13,604,229,361 bytes).",
     "fix": "No action needed for this copy. Just do not rely on `git status` as the guard when moving files into or out of demoSCSam3OneStageNew; use `du -sb` / `find -name '*.png' | wc -l` before and after."
    },
    {
     "severity": "note",
     "what": "My own `python -m py_compile` runs (task item d) wrote root-owned .pyc files into SCSam3/demoSCSam3OneStageNew/__pycache__ and SCSam3/demoSCSam3MVOpt/__pycache__, because the scsam3 container runs as root. I chowned both directories back to 1000:1000 afterwards.",
     "why": "Left as-is, the root-owned .pyc in demoSCSam3MVOpt/__pycache__ (a sjpark-owned dir) and the root-owned OneStageNew/__pycache__ dir itself would have needed sudo to clean up later.",
     "fix": "Already remediated: `docker run --rm -v /:/host scsam3 chown -R 1000:1000 <both __pycache__ dirs>`; both now show `sjpark sjpark`. Both are gitignored (`__pycache__/` in .gitignore), so git status is unaffected either way."
    }
   ],
   "evidence": [
    "(a) PASS — every .py is byte-identical. `cmp -s` over all 12 MVOpt .py files vs their OneStageNew namesakes: 12/12 IDENTICAL (SCSam3TrackerPredictor.py, SCSam3TrackerPredictorNewMem.py, SCSam3Video.py, SCSam3VideoInference.py, SCSam3VideoInferenceNewMem.py, SCSam3VideoPredictor.py, SCSam3VideoPredictorNewMem.py, build_scsam3.py, io_utils.py, misc.py, sam3_demoVideo.py, test.py). md5sums agree pairwise, e.g. SCSam3TrackerPredictor.py = ba8326a030521bffe9fe8e1ff2e35791 in both, SCSam3VideoInferenceNewMem.py = 23de1c7c23560733ff59cc4421e56f56 in both.",
    "(a) Set difference: no MVOpt .py is missing from OneStageNew. Exactly one file exists in one folder only — `SCSam3/demoSCSam3OneStageNew/SCSam3TrackerPredictorNewMem copy.py` (md5 1f5f2d1590187cb947106cc2e3af7f31, 97418 bytes, mtime Mar 10 18:53), which is the intended backup and is expected to be absent from MVOpt. `find -name '*.py' -not -path '*/__pycache__/*'` confirms neither folder has .py files in subdirectories, so the top-level comparison is complete. MVOpt has no non-.py entries other than __pycache__.",
    "(a) The six copied files are also identical to the COMMITTED MVOpt blobs, not just the working-tree ones: for each, `git show 3232d3e:SCSam3/demoSCSam3MVOpt/<f> | md5sum` == working-tree OneStageNew md5 == working-tree MVOpt md5. So MVOpt itself was not accidentally modified during the copy, and the snapshot corresponds to a committed state.",
    "(b) PASS — `git diff --name-status 3232d3e` returns exactly six lines, all `M`, all under SCSam3/demoSCSam3OneStageNew/: SCSam3TrackerPredictor.py, SCSam3TrackerPredictorNewMem.py, SCSam3Video.py, SCSam3VideoInference.py, SCSam3VideoInferenceNewMem.py, io_utils.py. `git diff --stat 3232d3e` = `6 files changed, 192 insertions(+), 44 deletions(-)`. No A (added), D (deleted), R (renamed) or mode-change entries anywhere.",
    "(b) Negative control: `git diff --stat 3232d3e -- . ':(exclude)<each of the six paths>'` produces EMPTY output — nothing else in the repo differs from 3232d3e. `git status --porcelain -uall` shows the same six ` M` lines and zero `??` untracked entries (`grep -c '^??'` = 0), so no stray file was dropped anywhere in the tree by the copy.",
    "(b) Diff content matches the seven documented optimizations and nothing else (372-line diff, 13 hunks): S1 sentinel `inference_state[\"previous_stages_out\"][frame_idx] = \"_THIS_FRAME_HAS_OUTPUTS_\"` at 4 sites (SCSam3VideoInference.py:1661 and :1912, SCSam3VideoInferenceNewMem.py:1787 and :2038); S2 mask_inputs_per_obj write removed at 2 sites (SCSam3VideoInference.py ~:1876, SCSam3VideoInferenceNewMem.py ~:2002); S3 `torch.empty(..., device=\"meta\")` placeholder at SCSam3TrackerPredictor.py:~407 and SCSam3TrackerPredictorNewMem.py:~407; S4 bool fast path in `_apply_non_overlapping_constraints` in both tracker files; S5 `img = img.to(torch.float32)` in io_utils.py:~206; S6 `uses_spatial_predictor = True` + `RetireSpatialPredictor()` in SCSam3Video.py; S7 `TRIM_CACHED_OUTPUTS` env flag + `_trim_cached_frame_outputs` + its one call site in SCSam3VideoInferenceNewMem.py. No unrelated edits, no debug prints, no path/config changes.",
    "(b) Sanity check on the S1 hunk (which deletes an `if \"previous_stages_out\" not in inference_state: ... = {}` guard): the key is unconditionally initialized as a LIST at SCSam3VideoInference.py:164 and SCSam3VideoInferenceNewMem.py:170 (`inference_state[\"previous_stages_out\"] = [None] * num_frames`), and all readers only compare elements against None (:188, :225, :233 and the NewMem equivalents :194, :231, :239). The removed dict-guard was dead/incorrect code; removing it is safe.",
    "(c) PASS — all 32 numbered directories 0..31 are present, each containing exactly 50 PNG files and no other files (32 x 50 = 1600 PNGs total, confirmed by `find SCSam3/demoSCSam3OneStageNew -type f -name '*.png' | wc -l` = 1600). Per-dir sizes ~435-456 MB; `du -sh` = 13G, `du -sb` = 13,604,229,361 bytes, and `du -sch` over just the numbered dirs is also 13G, i.e. the PNGs account for essentially the whole folder.",
    "(c) `.dockerignore` present (8 bytes, mtime Jul 29 13:24, tracked) and `SCSam3TrackerPredictorNewMem copy.py` present (97418 bytes, mtime Mar 10 18:53, tracked). `git diff --stat 3232d3e` restricted to those two paths is EMPTY — neither was touched by the copy.",
    "(d) PASS — `docker run --rm -v /:/host -w /host/home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew scsam3 ... python -m py_compile` on all 13 .py files: 13/13 OK under Python 3.12.3 (including 'SCSam3TrackerPredictorNewMem copy.py'). Same run against demoSCSam3MVOpt: 12/12 OK. No SyntaxError on any file.",
    "(d) Cross-check from the bytecode: the .pyc sizes produced in the two folders match exactly for every shared module (SCSam3TrackerPredictor 45055, SCSam3TrackerPredictorNewMem 60565, SCSam3Video 12587, SCSam3VideoInference 69183, SCSam3VideoInferenceNewMem 74800, io_utils 10297, ...), independently corroborating that the sources are identical.",
    "Post-verification state re-checked: md5sums of the six still match pairwise and `git status --porcelain -uall` still shows exactly the same six ` M` lines with no untracked entries — my verification runs changed nothing tracked."
   ]
  },
  {
   "axis": "runner-behavior",
   "ok": true,
   "confidence": "high",
   "findings": [
    {
     "severity": "minor",
     "what": "`MVSegVideo.spatial` (/home/sjpark/Documents/SCSegmentation/SCSam3/runMVSeg.py:141-144) is `getattr(self, \"predictor_spatial\", self.predictor)`. RetireSpatialPredictor sets the attribute to None rather than deleting it (SCSam3Video.py:69), so getattr FINDS it and returns None — it does NOT fall back to self.predictor. Verified by simulation: before retire -> 'SPATIAL_PREDICTOR', after retire -> None.",
     "why": "Any future post-retire use of `self.spatial` raises `AttributeError: 'NoneType' object has no attribute 'handle_request'`. Harmless today only because the four call sites (runMVSeg.py:180, :184, :190, :210) all sit in LoadCameraFolders/AddReferenceMask/PropagateAcrossViews, which run before the retire at :336. The RetireSpatialPredictor docstring (SCSam3Video.py:45-48) justifies None-not-delete as keeping 'an older caller sniffing hasattr' on the multi-session branch — that reasoning covers hasattr but silently breaks runMVSeg's own getattr-with-default property.",
     "fix": "Change the property to `p = getattr(self, \"predictor_spatial\", None); return p if p is not None else self.predictor`, so it degrades to the temporal predictor instead of None."
    },
    {
     "severity": "note",
     "what": "The comment at /home/sjpark/Documents/SCSegmentation/SCSam3/runMVSeg.py:249-250 — \"(OneStage has neither; OneStageNew has only the attribute)\" — is now factually wrong for OneStageNew. After the copy, demoSCSam3OneStageNew/SCSam3Video.py:23 sets `self.uses_spatial_predictor = True`, so OneStageNew has BOTH the flag and the attribute. The OneStage half is still correct (demoSCSam3OneStage/SCSam3Video.py:16-29 builds only self.predictor, no flag).",
     "why": "The comment is the only in-code documentation of why the branch tests the flag first; a reader trusting it would conclude OneStageNew still relies on the attribute fallback and might 'simplify' the guard back to a bare hasattr, which would break OneStageNew (see the KeyError analysis in evidence).",
     "fix": "Update to '(OneStage has neither; OneStageNew and MVOpt both set the flag)'."
    },
    {
     "severity": "note",
     "what": "/home/sjpark/Documents/SCSegmentation/SCSam3/runMVOptThree.sh:14 sets `-e SCSAM3_TRIM_CACHED_OUTPUTS=1` unconditionally, and :16 takes `--algo \"${ALGO:-MVOpt}\"`. So `ALGO=OneStageNew ./runMVOptThree.sh ...` now activates S7 trimming inside OneStageNew. Before the copy that env var was inert there — demoSCSam3OneStageNew/SCSam3VideoInferenceNewMem.py had no TRIM_CACHED_OUTPUTS code at all.",
     "why": "OneStageNew launched through that script is no longer an un-optimized control run; it is MVOpt behaviour under a different name. This is exactly what the in-flight verification (PID 1196062) is doing, and its output is byte-identical to the pre-copy reference, so nothing is wrong — but anyone using OneStageNew as a baseline via runMVOptThree.sh should know the flag is on.",
     "fix": "No code change needed given the frozen-snapshot intent; if a true baseline is ever wanted, unset the var for non-MVOpt algos or run through runMVSegAll.sh, which never sets it."
    },
    {
     "severity": "note",
     "what": "/home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew/SCSam3TrackerPredictorNewMem copy.py (97418 bytes, mtime 2026-03-10) survived the copy as required, but is now a stale backup of superseded code: it has 0 occurrences of `device=\"meta\"` while the live SCSam3TrackerPredictorNewMem.py has 1.",
     "why": "It sits next to the live file with a near-identical name and now silently disagrees with it on S3 and S4. It cannot shadow anything (the space in the filename makes it unimportable, and nothing references it — grep found no importer), so this is documentation debt, not a break.",
     "fix": "Either delete it or rename it to something that marks it as pre-optimization, e.g. `SCSam3TrackerPredictorNewMem.pre-MVOpt.py.bak`."
    }
   ],
   "evidence": [
    "(a) BRANCH SHAPE UNCHANGED. runMVSeg.py:251-256 is `if getattr(self, \"uses_spatial_predictor\", getattr(self, \"predictor_spatial\", None) is not None): request[\"session_ids\"]=...; request[\"spatial_idx\"]=m  else: request[\"session_id\"]=...`. Simulated all four package states with the exact expression: OneStageNew BEFORE copy -> MULTI (session_ids+spatial_idx); OneStageNew AFTER copy (retired) -> MULTI; OneStage -> SINGLE. Identical before and after, by two different routes: BEFORE, `uses_spatial_predictor` was absent so the eagerly-evaluated fallback ran, and since no RetireSpatialPredictor existed `predictor_spatial` was still a live object -> True. AFTER, SCSam3Video.py:23 `self.uses_spatial_predictor = True` is found by the outer getattr and short-circuits the (now False) fallback -> True.",
    "(a) COUNTERFACTUAL — Retire WITHOUT the flag. Simulation: `o.predictor_spatial = None` with no flag -> SINGLE branch, i.e. `request[\"session_id\"]`. But SCSam3VideoPredictorNewMem.py:95-107 `handle_stream_request` does `session_ids=request[\"session_ids\"]` and `spatial_idx=request[\"spatial_idx\"]` with hard subscripts -> KeyError: 'session_ids'. Because both handle_stream_request layers are generator functions (`yield from` at :99 and :372), the KeyError would NOT surface at runMVSeg.py:257 where the generator object is created; it surfaces at the first `next(sc.tracking_result[j])` in the write loop, runMVSeg.py:344 — i.e. after LoadCameraFolders, the 16-object prompt, the full 46-view cross-view propagation and all of TrackForward's add_prompt calls had already burned, and before a single PNG is written.",
    "(b) GUARD NOW FIRES — code. `git show HEAD:SCSam3/demoSCSam3OneStageNew/SCSam3Video.py | grep -n 'RetireSpatialPredictor\\|uses_spatial_predictor'` returns nothing (exit 1): both were absent before the copy, so `hasattr(sc, \"RetireSpatialPredictor\")` at runMVSeg.py:335 was False and the block was skipped. After the copy the method is at SCSam3Video.py:37-72 and the flag at :23.",
    "(b) GUARD NOW FIRES — empirical. SCSam3/logs/mvopt-20260904-111149/Welder.log prints, in order: `algorithm      OneStageNew  (demoSCSam3OneStageNew)` / `sessions ready (46 cameras loaded, 46 tracked [all], 2560x1920)` / `prompted       16 objects on view 0` / `cross-view     done, 46 views have masks` / `spatial model  retired` / `frame 0 written` ... `frame 13 written`. No Traceback, no KeyError in the log. This run is still in flight (ps: PID 1196062, `docker run ... -e SCSAM3_TRIM_CACHED_OUTPUTS=1 ... python runMVSeg.py Welder --algo OneStageNew --out SegMaskSam3OneStageNew_verify --overwrite`).",
    "(b) OUTPUT UNCHANGED. `diff -r -q Data/MVSeg/Welder/SegMaskSam3OneStageNew_verify Data/MVSeg/Welder/SegMaskSam3OneStageNew | grep -c '^Files'` -> 0. All 677 PNGs written so far by the post-copy run are byte-identical to the pre-copy reference (reference mtime 2026-09-03 12:03:32; the copy landed 2026-09-04 01:00:31, the same mtime on both demoSCSam3OneStageNew/SCSam3Video.py and demoSCSam3MVOpt/SCSam3Video.py). The only diff lines are 'Only in SegMaskSam3OneStageNew/camera_000X: 15..20' — frames the in-flight run has not reached yet.",
    "(b) EVERY POST-RETIRE READ OF predictor_spatial, exhaustively. Only one exists: runMVSeg.py:252, inside TrackForward (called at :340, after the retire at :336). Python evaluates a getattr default eagerly, so `getattr(self, \"predictor_spatial\", None) is not None` DOES execute and DOES read the None attribute — but the result is discarded because the outer getattr finds `uses_spatial_predictor`. No crash, no behaviour change. Nothing else: main() touches only LoadCameraFolders(:309), AddReferenceMask(:318), PropagateAcrossViews(:321), RetireSpatialPredictor(:336), TrackForward(:340), plus attributes numImage/video_width/video_height/obj_ids/masks_spatial/track_views/tracking_result. The base-class methods that use predictor_spatial — LoadVideo_Folder(:79-80), LoadVideo_Folder_MVSeg(:90-91), LoadVideo_File(:140,:149), AddPoint(:172), AddText(:186), InitializeSegmentation(:216) in demoSCSam3OneStageNew/SCSam3Video.py — are none of them on the runMVSeg path (MVSegVideo supplies its own LoadCameraFolders/AddReferenceMask/PropagateAcrossViews/TrackForward). `session_id_statial`, also nulled (SCSam3Video.py:56), is read only at runMVSeg.py:183/186/192/206, all pre-retire.",
    "(b) masks_spatial SURVIVES RETIREMENT. runMVSeg.py:213-216 stores `out[\"out_binary_masks\"][i] > 0.0`, and `out_binary_masks` is already a HOST numpy array — SCSam3VideoInference.py:517 `\"out_binary_masks\": out_binary_masks.cpu().numpy()` (same at SCSam3VideoInferenceNewMem.py:524). So masks_spatial holds fresh host arrays with no reference to any GPU tensor, session, or model weight that RetireSpatialPredictor frees. TrackForward's re-seed at runMVSeg.py:229-236 is bit-identical.",
    "(c) MVOpt/OneStage UNAFFECTED. `git status --short` lists exactly six modified files, all under SCSam3/demoSCSam3OneStageNew/; nothing under demoSCSam3MVOpt/ or demoSCSam3OneStage/. build_runner (runMVSeg.py:116-125) filters HERE out of sys.path, inserts ONLY `os.path.join(HERE, ALGOS[algo])` and chdirs there, so an MVOpt or OneStage run never imports demoSCSam3OneStageNew. OneStage specifically: demoSCSam3OneStage/SCSam3Video.py:16-29 builds only `self.predictor` (no predictor_spatial) and sets no flag -> branch :251 is False -> `request[\"session_id\"]`, which matches demoSCSam3OneStage/SCSam3VideoPredictor.py:100 `session_id=request[\"session_id\"]`. Its RetireSpatialPredictor (:31-66) does fire at :335 but returns immediately at :47-48 (`getattr(...) is None`). That is pre-existing, committed at 3232d3e. Logs corroborate: 15 OneStage logs and 16 MVOpt logs already contain 'spatial model  retired' with no error.",
    "(d) runMVSeg.py IS LITERALLY UNCHANGED. `git diff HEAD -- SCSam3/runMVSeg.py` produces zero output; the file is tracked (`git ls-files` confirms). Semantically, per item: track_mode/track_idx (:304-306) depend only on args.track_cams and the NEEDS_ALL_VIEWS tuple (:46), pure Python with no reference to the demo package — OneStageNew still resolves to 'all', confirmed by the log line 'sessions ready (46 cameras loaded, 46 tracked [all], ...)'. pick_reference (:95-111) is pure cv2/numpy over ground-truth PNGs and runs at :280, BEFORE build_runner at :300, so the demo package cannot reach it at all. The write loop (:342-356) reads response['frame_index'], out['out_obj_ids'], out['out_binary_masks'] — the output dict shape at SCSam3VideoInferenceNewMem.py:522-527 is untouched by the diff.",
    "(d) SPATIAL_START_IMPLICIT SEMANTICS PRESERVED under S3. Default '1' (runMVSeg.py:37) means `start_frame_index` is omitted (:208-209), so the predictor takes start_frame_idx=None and falls to _get_processing_order, SCSam3VideoInference.py:225 `if all(out is None for out in previous_stages_out) and start_frame_idx is None` and :233 `min(t for t, out in enumerate(previous_stages_out) if out is not None)`. Both readers test ONLY against None. S3 replaced the stored `out` dict with the string '_THIS_FRAME_HAS_OUTPUTS_' (:1661, :1912) — still non-None at the same indices — so the same start view is selected. previous_stages_out is a pre-sized list (`[None] * num_frames`, :164), which also makes the deleted `if \"previous_stages_out\" not in inference_state: = {}` guard dead code, not a lost initialization.",
    "(e) OTHER IMPORTERS — full-tree grep. `grep -rn demoSCSam3OneStageNew --include=*.py --include=*.sh --include=*.json --include=*.yml --include=*.yaml --include=Dockerfile --include=*.cfg --include=*.toml --include=*.txt` over the repo yields exactly ONE code hit: SCSam3/runMVSeg.py:39. Everything else is under docs/. In-directory importers (they run with the folder as cwd): demoSCSam3OneStageNew/sam3_demoVideo.py:3 `from SCSam3Video import SCSam3Video` — does NOT break, because the SCSam3Video.py diff is purely additive (one line in __init__ at :23, one new method at :37-72) and everything sam3_demoVideo calls (LoadVideo_File, AddText, InitializeSegmentation, RunNaiveTracking) is untouched and never retires the spatial predictor. demoSCSam3OneStageNew/test.py imports nothing from the package. SCSam3/runMVSegAll.sh:25-26 and SCSam3/waitAndRunMVSeg.sh:55 reference only the algo NAME and go through runMVSeg.py. SCSam3/Dockerfile COPYs only ./sam3, never the demo dirs, and the container mounts the host at /host, so demo code is read live off disk — no stale image. demoSCSam3OneStageNew/.dockerignore ('*' then '!*.py') is inert: docker honours only a .dockerignore at the build-context root, and the sole Dockerfile is SCSam3/Dockerfile.",
    "COPY COMPLETENESS. All 12 .py files now compare byte-identical between demoSCSam3MVOpt and demoSCSam3OneStageNew (cmp on each). `diff <(ls MVOpt/*.py) <(ls OneStageNew/*.py)` shows one extra file only: 'SCSam3TrackerPredictorNewMem copy.py'. All required survivors are intact: 32 numbered dirs (13G total), .dockerignore (8 bytes, mtime Jul 29), and the backup copy.py. `python3 -m py_compile` on all six copied files succeeds."
   ]
  },
  {
   "axis": "figures-assets",
   "ok": true,
   "confidence": "high",
   "findings": [
    {
     "severity": "note",
     "what": "The figure pipeline's entire input and output set is invisible to git: `.gitignore:183` is `*.png`, `git ls-files '*.png'` returns 0, and `git log --all --name-only -- '*.png'` returns 0 — no PNG has ever been committed in the repo's history. `git ls-files SCSam3/demoSCSam3OneStageNew` lists exactly 14 files, all .py plus .dockerignore; the 32 numbered directories (1600 PNGs, 13 GB) are entirely untracked.",
     "why": "If demoSCSam3OneStageNew/0..31 is ever deleted, both make_figure.py and make_baseline_figure.py die with FileNotFoundError and the six paper figures become unreproducible without re-running sam3_demoVideo.py on GPU. Git offers no recovery path whatsoever. This is exactly why copying only the six .py files, rather than rsync-ing the directory, was necessary — a directory copy with --delete would have destroyed 13 GB of unrecoverable assets.",
     "fix": "The figure scripts only need the 32 first frames `SCSam3/demoSCSam3OneStageNew/<m>/0.png` — 132 MB total (`du -ch SCSam3/demoSCSam3OneStageNew/*/0.png`). Either `git add -f` those 32 files (or add a `!*/0.png` negation) so the teaser/baseline figures stay reproducible from a clean clone, or back the directory up outside the repo. The remaining 1568 PNGs (frames 1..49) are not read by either script and need not be preserved for figures."
    },
    {
     "severity": "note",
     "what": "`figures/baseline_independent/` (32 PNGs + detections.json, 49 MB) has the same exposure — ignored by `.gitignore:183`, untracked — and its producer `figures/run_independent_baseline.py:26-27` imports `sam3.build_sam3_image_model` / `Sam3Processor`, so regenerating it requires the model weights and a GPU container, not just PIL.",
     "why": "Row (a) of fig_baseline_*.png is unrecoverable from git and expensive to regenerate. Not touched by this copy, but it is the second single-point-of-failure in the same pipeline.",
     "fix": "Back up `figures/baseline_independent/` alongside the 32 reference frames, or force-add it (49 MB)."
    }
   ],
   "evidence": [
    "(a) Paths read from demoSCSam3OneStageNew — make_figure.py:14 `SRC = \"/home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew\"`; line 132 `paste(f\"{SRC}/{REF_VIEW}/0.png\", ...)` (REF_VIEW=0, line 23); line 160 `paste(f\"{SRC}/{m}/0.png\", ...)` for m in range(32) (N_VIEWS=32, line 22). make_baseline_figure.py:15 `OURS = \".../demoSCSam3OneStageNew\"`; line 119 `path = f\"{BASE}/{v}.png\" if r == 0 else f\"{OURS}/{v}/0.png\"` for v in VIEWS (line 22, default \"0 1 2 3 4 5 6 7\"). Union of required paths = demoSCSam3OneStageNew/<m>/0.png for m=0..31. Neither script imports anything from that directory — they read image bytes only.",
    "(a) All 32 exist: loop over `seq 0 31` testing `-f SCSam3/demoSCSam3OneStageNew/$m/0.png` printed no MISSING lines, `missing_flag=0`. `ls -d [0-9]*` in demoSCSam3OneStageNew lists 0..31, count=32; `find . -name '*.png' | wc -l` = 1600; `du -sh .` = 13G. Unique survivors intact: `.dockerignore` (8 bytes, Jul 29) and `SCSam3TrackerPredictorNewMem copy.py` (97418 bytes, Mar 10). Aux inputs also present: figures/baseline_independent/ has 33 files (0.png..31.png + detections.json); detections.json parses, 32 entries, views 0-7 -> n = {0:5, 1:7, 2:6, 3:7, 4:6, 5:6, 6:5, 7:7}. Fonts present: /usr/share/fonts/opentype/noto/NotoSansCJK-{Bold,Regular}.ttc.",
    "(b) Container NOT needed. Imports are stdlib + PIL only (make_figure.py:10-12 os/sys/PIL; make_baseline_figure.py:11-13 json/os/sys/PIL). System `/usr/bin/python3` is 3.12.3 with PIL 10.2.0 — ran both scripts directly, no container.",
    "(b) Both scripts ran clean, exit 0, no traceback. make_figure.py output: `fig_teaser_ko.png (3400, 1010) 2.2 MB`, `fig_teaser_en.png (3400, 1010) 2.2 MB`, `fig_teaser_nocap.png (3400, 640) 2.2 MB`. make_baseline_figure.py output: `fig_baseline_ko.png (3400, 818) 1.7 MB`, `fig_baseline_en.png (3400, 818) 1.6 MB`, `fig_baseline_nocap.png (3400, 520) 2.1 MB`.",
    "(b) All six regenerated PNGs are BYTE-IDENTICAL to the committed-on-disk figures (md5sum + size, old vs new): fig_teaser_ko 1f0ba4f07291d7380a7b4b7cbfc0ec2c / 2185117 both; fig_teaser_en 5069ca9a4f8f1e241849c93601b23d91 / 2175840 both; fig_teaser_nocap f4d335f2af13d17502b3932de48ed89e / 2231559 both; fig_baseline_ko 8ddc498ceb7bbf0b60b20b8649ad7032 / 1659668 both; fig_baseline_en 7343fdd046fa1ae5d7354c17994556c1 / 1645461 both; fig_baseline_nocap 130ca6fa6258227e619eb88b090d6ede / 2106758 both. Source PNGs are untouched by the copy — mtime 2026-03-11 08:22-08:23, predating the existing figures (2026-09-02 13:39 / 13:55).",
    "(c) Not recoverable from git. `grep -n png .gitignore` -> `183:*.png`. `git check-ignore -v` confirms rule .gitignore:183 matches SCSam3/demoSCSam3OneStageNew/{0,16,31}/0.png, figures/fig_teaser_ko.png and figures/baseline_independent/0.png. `git ls-files '*.png' | wc -l` = 0. `git log --all --name-only --pretty=format: -- '*.png' | sort -u | grep -c .` = 0 — no PNG in any commit on any branch. `git ls-files SCSam3/demoSCSam3OneStageNew` = 14 entries, all .py + .dockerignore, no numbered directories.",
    "Copy itself is consistent with the stated intent: `git diff --stat -- SCSam3/demoSCSam3OneStageNew` touches exactly the 6 named .py files (192 insertions, 44 deletions) and nothing else; `cmp` reports all six byte-identical to their demoSCSam3MVOpt counterparts."
   ]
  },
  {
   "axis": "repo-regression",
   "ok": false,
   "confidence": "high",
   "findings": [
    {
     "severity": "blocker",
     "what": "`SCSam3/waitAndRunMVSeg.sh` runs `runMVSegAll.sh --overwrite` twice (line 55 smoke test on Frog, line 63 full sweep). `runMVSegAll.sh:25-26` has `ALGOS=(OneStage OneStageNew)` and `OUTNAME[OneStageNew]=SegMaskSam3OneStageNew`, so the sweep writes patched code into the published-baseline mask directories. The only guard, `runMVSeg.py:294-295` (`already exists and is not empty; pass --overwrite`), is explicitly bypassed by that flag.",
     "why": "One invocation of `./waitAndRunMVSeg.sh` overwrites all 12 stored `SegMaskSam3OneStageNew` directories (12,692 PNGs) with output from the memory-patched code, under the baseline's own name. The pre-copy code no longer exists anywhere on disk — I verified md5 `0bcbaed127a9ce96c18b49e18ad319b6` (3232d3e's `demoSCSam3OneStageNew/SCSam3TrackerPredictor.py`) matches none of the 7 on-disk copies, and `/tmp/sibling_backup/` holds ForSam2, ForSam2New, OneStage, TwoStage, TwoStageNew but NOT OneStageNew. Recovery is git-only.",
     "fix": "Before anything else, either commit the copy so 3232d3e stays the recoverable reference and add a banner to `waitAndRunMVSeg.sh` saying the sweep now regenerates OneStageNew with MVOpt code, or drop `OneStageNew` from `runMVSegAll.sh:25` and point the sweep at `MVOpt`. Also copy the 3232d3e version of the 6 files to an out-of-/tmp backup."
    },
    {
     "severity": "blocker",
     "what": "`SCSam3/runMVOptThree.sh` sets `-e SCSAM3_TRIM_CACHED_OUTPUTS=1` unconditionally (line 14) and runs `--algo \"${ALGO:-MVOpt}\" ${OUT:+--out \"${OUT}\"} --overwrite` (line 16). `SCSAM3_TRIM_CACHED_OUTPUTS` now exists in `demoSCSam3OneStageNew/SCSam3VideoInferenceNewMem.py:38` (absent at 3232d3e — `git show 3232d3e:...` returns nothing for that grep).",
     "why": "`ALGO=OneStageNew ./runMVOptThree.sh Blocks` now (a) activates S7 trim inside the OneStageNew package, which the docs classify as `새 기능` / not output-neutral (memory-optimization.md:163-164), and (b) writes with `--overwrite` into `SegMaskSam3MVOpt`... no, into `DEFAULT_OUT[OneStageNew]` = `SegMaskSam3OneStageNew`, destroying the baseline. `docs/raw/runner-gotchas.md:260` documents the exact opposite behaviour: \"Running that script with `ALGO=OneStageNew` silently drops the memory fix.\" It now silently *enables* it.",
     "fix": "Gate the `-e` on `${ALGO}` being MVOpt, or make `--overwrite` conditional. Update runner-gotchas.md:260."
    },
    {
     "severity": "blocker",
     "what": "`docs/memory-optimization.md` per-folder application matrix is now factually inverted for OneStageNew. Line 194: `| demoSCSam3OneStageNew | — | — | — | — | — | — | — |`. Line 196: \"**MVOpt만 7종 전부를 갖습니다. OneStageNew는 하나도 없습니다** — 무패치 기준 폴더입니다.\" Line 9-10: \"`SCSam3/demoSCSam3MVOpt/` — `demoSCSam3OneStageNew`의 바이트 사본에서 시작했습니다. 원본은 손대지 않았습니다.\"",
     "why": "I measured every marker on disk: OneStageNew and MVOpt now score identically on all seven (S1 sentinel 3+3, S2 leftover write 0, S3 `device=\"meta\"` 2, S4 `background_value == 0` 4, S5 `img = img.to(torch.float32)` 1, S6 `uses_spatial_predictor = True` 1, S7 `SCSAM3_TRIM_CACHED_OUTPUTS` 2). `[MEM]` tag count is 2 in both folders. Anyone using this matrix to decide what to port, or to explain a memory result, gets the wrong answer for the folder the whole document is anchored on.",
     "fix": "Change row 194 to all ✓, delete the \"OneStageNew는 하나도 없습니다 — 무패치 기준 폴더입니다\" sentence, and rewrite lines 9-10 to say the original WAS overwritten on 2026-09-04 and lives only at commit 3232d3e."
    },
    {
     "severity": "blocker",
     "what": "`docs/raw/patch-matrix.md` is wrong in six places. Line 28-29: `| **OneStageNew** | SCSam3VideoInference.py | 1 | 1 | 0 | 0 | 0 | 0 | 0 |` and the NewMem row. Line 48: `| demoSCSam3OneStageNew | ABSENT | ABSENT | ABSENT | ABSENT | ABSENT | ABSENT | ABSENT |`. Line 52: \"**`demoSCSam3MVOpt` is the only folder carrying all seven.** `demoSCSam3OneStageNew` carries none — it is the unpatched reference.\" Line 72: \"...1 hit each in MVOpt/..., TwoStageNew/SCSam3VideoInference.py; 0 in OneStageNew and 0 in ForSam2*.\" Line 76: \"`demoSCSam3OneStageNew/SCSam3VideoInferenceNewMem.py` ... has ... zero trim hits.\" Line 80: \"`diff -rq --exclude=__pycache__ demoSCSam3OneStageNew demoSCSam3MVOpt` differs on exactly 5 `.py` files\".",
     "why": "Every one of these is now measurably false. `diff -rq` between the two folders currently reports 0 differing files. `[MEM]` is 2 in OneStageNew, not 0. Trim hits are 2, not 0. This file is cited by memory-optimization.md as the raw evidence base, so the falsification propagates.",
     "fix": "Rewrite lines 28-29, 48, 52, 72, 76 and 80 with the post-copy measurements, and add a dated header saying the matrix describes the tree at commit 3232d3e."
    },
    {
     "severity": "important",
     "what": "`docs/operations.md` trap 4 and trap 5 both assert the absence of S6 in OneStageNew. Line 121: `| OneStageNew | 있음 | 없음 | 없음 |`. Line 125: \"OneStageNew는 플래그가 없어 속성 대체 경로로 같은 분기를 탑니다.\" Line 133-134: \"**OneStageNew**는 메서드 자체가 없어 약 3.2 GiB 교차시점 모델이 시간 추적 내내 상주합니다. 이것이 OneStageNew가 무거운 데이터셋에서 터지는 이유 중 하나입니다.\"",
     "why": "`demoSCSam3OneStageNew/SCSam3Video.py:23` now sets `uses_spatial_predictor = True` and line 37 defines `RetireSpatialPredictor`. So the runner takes the flag branch, not the fallback, and `runMVSeg.py:335-338` now actually retires the 3.2 GiB model for `--algo OneStageNew`. The stated cause of OneStageNew's OOM behaviour no longer applies to the code in that folder — anyone debugging a heavy dataset with this doc open will be reasoning about code that is gone.",
     "fix": "Row 121 becomes `| OneStageNew | 있음 | **있음** | **있음** |`; delete line 125's second clause; rewrite 133-134 in the past tense scoped to 3232d3e."
    },
    {
     "severity": "important",
     "what": "`SCSam3/runMVSeg.py` carries two now-false comments about its own algo table. Line 249-250: \"The attribute check is the fallback for packages that do not set the flag (OneStage has neither; OneStageNew has only the attribute).\" Line 44-45: \"MVOpt is OneStageNew with the memory fixes; it shares the cross-view memory design...\"",
     "why": "OneStageNew now sets the flag, so it no longer uses the fallback. Separately, \"OneStage has neither\" was already wrong before the copy — `demoSCSam3OneStage/SCSam3Video.py:31` defines `RetireSpatialPredictor` (present at 3232d3e; that folder is clean in git status). And `MVOpt is OneStageNew with the memory fixes` is now vacuous: the two folders are byte-identical, so MVOpt is not OneStageNew *plus* anything.",
     "fix": "Rewrite the :249-250 parenthetical to \"(OneStage defines the method but never sets the flag and has no predictor_spatial; OneStageNew and MVOpt both set the flag)\". Rewrite :44-45 to note the two ALGOS keys now resolve to identical code and differ only in DEFAULT_OUT."
    },
    {
     "severity": "important",
     "what": "`docs/raw/runner-gotchas.md` line 204 table row: `| demoSCSam3OneStageNew | yes (SCSam3Video.py:16) | **no** | **no** | multi-session, via the attribute fallback |`; line 224: \"**`OneStageNew` never retires anything** — it has no such method, so the ~3.2 GiB cross-view model ... stay resident for the whole temporal pass. Only `OneStage` and `MVOpt` define the method.\"; line 260 (quoted in the earlier finding); line 266: \"MVOpt adds `uses_spatial_predictor` and `RetireSpatialPredictor`, which OneStageNew lacks entirely. The runner's own fallback comment at `:249-250` states this correctly.\"",
     "why": "All four are false as of the copy, and 266 additionally certifies a runner comment that is itself now wrong, so a reader chasing the discrepancy is sent to a second wrong source.",
     "fix": "Update rows 204/224/260/266 and add the same 3232d3e-scoping header."
    },
    {
     "severity": "important",
     "what": "`docs/analysis/SCHEDULE.md` states the no-touch policy as a hard invariant that has now been violated. Line 9: \"**원본(`demoSCSam3OneStageNew`, `demoSCSam3OneStage`, 그 외 sibling 폴더)은 절대 건드리지 않는다.** 게시된 J&F 숫자가 그 폴더들의 출력에 묶여 있고, 문제가 생기면 즉시 되돌아갈 기준점이 필요하다.\" Line 352: \"...가 같은 저자 패치를 갖고 있으나 **의도적으로 건드리지 않는다.** 원본 보존이 목적이다.\" Line 243: \"`OneStageNew`는 속성이 있고 `None`이 아니며 `RetireSpatialPredictor`가 없으므로 `hasattr` 가드에 걸려 은퇴 코드가 실행되지 않는다 → 완전히 동일.\"",
     "why": "Line 243 is the written argument that the S6 runner edit was neutral for OneStageNew. That argument rested specifically on the method NOT existing in the package. The copy removes its premise, so the neutrality claim for `--algo OneStageNew` is now unsupported by the document that established it (the empirical 12,428-PNG rerun still supports output-neutrality, but the *stated reasoning* is void).",
     "fix": "Add a dated amendment at SCHEDULE.md:9 recording that the policy was deliberately retired on 2026-09-04, and rewrite :243 to say the retire path now DOES run for OneStageNew and is covered by the 12-dataset byte-equality evidence instead."
    },
    {
     "severity": "important",
     "what": "`docs/README.md` code-location table. Line 52: \"| `SCSam3/demoSCSam3OneStageNew/` | 기준 알고리즘 — 교차시점 메모리 추적. 발표 결과를 만든 코드 |\". Line 53: \"| `SCSam3/demoSCSam3MVOpt/` | 위의 사본 + 메모리 수정. **알고리즘 동일**, 출력 동일 |\". Line 34: \"| [diffs/](diffs/) | upstream SAM 3 대비 알고리즘 diff 9종 + MVOpt vs OneStageNew diff |\".",
     "why": "Line 52's \"발표 결과를 만든 코드\" (the code that produced the published results) is the single most load-bearing sentence in the docs about this folder, and it is now false — that code is only at 3232d3e. Line 53 describes MVOpt as OneStageNew *plus* fixes when the two are byte-identical. Line 34 advertises a diff that can no longer be regenerated: `diff -r` between the folders now returns 0 differing files, so `docs/diffs/mvopt_vs_onestagenew.diff` is an orphaned artefact with no live source.",
     "fix": "Line 52 → \"기준 알고리즘. 2026-09-04 MVOpt 사본으로 덮어씀; 발표 결과를 만든 코드는 커밋 3232d3e에만 있음\". Line 53 → \"OneStageNew와 바이트 동일 (개발은 여기서)\". Line 34 → note the diff is a frozen record of the 3232d3e→MVOpt delta."
    },
    {
     "severity": "important",
     "what": "`docs/mvopt-audit.md` frames a decision that has already been made, and one of its evidence rows now mislabels the code. Line 3: \"MVOpt는 OneStageNew에 메모리 수정만 넣은 것인가? 그대로 되돌려 넣어도 되는가?\" Line 35: \"**새로 생기는 실패 양상** (OneStageNew에는 없던 것):\" followed by three failure modes (post-retire `predictor_spatial` is None so `runMVSeg.py:144`'s fallback misfires; `AddPoint`/`AddText`/`InitializeSegmentation`/`LoadVideo_*` die after retire; a single instance cannot run cross-view propagation twice). Line 96: \"| MVOpt(**트림 ON**) vs OneStageNew(트림 기능 없음) | 12개 데이터셋 · PNG 12,692장 | **차이 0** |\".",
     "why": "The three \"new\" failure modes at :35 are now live in OneStageNew too, and the doc still presents them as reasons to think carefully *before* copying — the reader is not told the copy already happened. Line 96's \"(트림 기능 없음)\" is true of the code that produced those stored PNGs but false of the folder today, which is exactly the ambiguity that makes the evidence row unverifiable now.",
     "fix": "Add a dated banner at the top: \"2026-09-04: the answer was yes; the copy was performed. Everything below describes the pre-copy state.\" Change :96's parenthetical to \"(트림 기능 없음, 커밋 3232d3e 코드)\"."
    },
    {
     "severity": "important",
     "what": "`docs/investigations-closed.md` section 11-A (line 240-260) is no longer reproducible from the working tree. Line 242: \"MVOpt 동등성 검증을 위해 OneStageNew를 12개 데이터셋에 다시 돌렸습니다.\" Line 253: \"그 출력은 `logs/mvseg-20260903-114432/OneStageNew-Welder.log`, 즉 **메모리 상한이 없는 `runMVSegAll.sh`** 로 만들어졌습니다.\" Line 255: \"즉 OneStageNew는 Welder에서 90 GiB를 넘게 씁니다.\" Line 258: \"**이유는 알고 있습니다.** OneStageNew에는 `RetireSpatialPredictor`가 없어 약 3.2 GiB 교차시점 모델과 46시점 유사 비디오, 특징 캐시가 시간 추적 내내 상주합니다.\" Also section 9 line 200: \"| **OneStageNew 신규 재실행 vs MVOpt 저장본** (2026-09-04) | PNG 12,428장 | **0** |\".",
     "why": "Re-running `--algo OneStageNew` under `--memory=90g` today will now COMPLETE Welder, because the folder holds MVOpt code. The doc's headline finding (\"OneStageNew는 Welder를 90 GiB 안에서 완주하지 못합니다\") will appear refuted by anyone who tries to reproduce it, with no marker explaining why. Same for the 12,428-PNG equivalence rerun: repeating it now compares patched against patched and proves nothing.",
     "fix": "Scope both sections explicitly: \"(코드: 커밋 3232d3e의 `demoSCSam3OneStageNew`. 2026-09-04 이후 그 폴더는 MVOpt 사본이므로 이 실험은 그 커밋을 체크아웃해야 재현됩니다.)\""
    },
    {
     "severity": "important",
     "what": "The copy preserved MVOpt's mtimes on the six overwritten files, creating a false forensic trail. `SCSam3/demoSCSam3OneStageNew/*.py` now read `2026-09-04 01:00` / `01:02`, identical to `demoSCSam3MVOpt/*.py`. The OneStageNew verification rerun that produced `SegMaskSam3OneStageNew_verify` finished at `2026-09-04 10:35` (log `SCSam3/logs/onestagenew-verify-101913.log`).",
     "why": "mtime is the only provenance signal on these paths (`Data/` is gitignored at `.gitignore:172`, `logs/` likewise). Reading `ls -l` today, the patched files look like they were in place nine hours BEFORE the verification run — which would mean the equivalence rerun was itself run on patched code and proves nothing. It was not, but nothing on disk says so.",
     "fix": "Touch the six files to the actual copy time, or better, record the copy in a `docs/PROVENANCE.md` line with the commit hash and wall-clock time, since mtime cannot be trusted after any `cp -p`."
    },
    {
     "severity": "important",
     "what": "`docs/experiments.md` derives three published caveats from OneStageNew's inability to finish the heavy datasets. Line 52: \"제외: AlexaMeadeExhibit, CoffeeMartini, FlameSteak (OneStageNew가 완주하지 못한 것들).\" Line 75: \"OneStageNew는 무거운 3개를 **완주한 적이 없고**, 해당 마스크 폴더가 디스크에 존재하지 않습니다.\" Line 79: \"하지만 **\\\"OneStageNew가 15개를 돌았다\\\"고 쓰면 사실이 아닙니다.**\" Line 141: \"| `SegMaskSam3OneStageNew` | 12 | 무거운 3개는 완주 불가 |\"",
     "why": "All four are true of the historical runs but false as statements about the current package: `--algo OneStageNew` can now finish all 15. Worse, the plain-`runMVSegAll.sh` path (no `--overwrite`) is blocked by `runMVSeg.py:294-295` only for the 12 directories that already exist — for AlexaMeadeExhibit, CoffeeMartini and FlameSteak the directories are absent, so a default sweep will silently CREATE `SegMaskSam3OneStageNew` masks for exactly the three datasets the paper says OneStageNew could never finish, retroactively 'validating' `jf_sam3_onestagenew_full.json` with MVOpt-computed output.",
     "fix": "Add \"(3232d3e 코드 기준)\" to :52/:75/:141, and either delete the OneStageNew column per the doc's own advice at :71 (\"논문에는 **한 열로만** 쓰십시오\") or block the three datasets from the OneStageNew sweep."
    },
    {
     "severity": "minor",
     "what": "`docs/open-items.md` §4 (lines 46-55) still poses the copy as an open question: \"## 4. MVOpt를 OneStageNew에 반영할 것인가\", \"S6 (`RetireSpatialPredictor`) — 넣는 순간 `runMVSeg.py`가 OneStageNew에 대해 다르게 동작합니다\", \"**추가 고려.** OneStageNew는 git 추적 중이고 발표된 기준선을 만든 코드입니다. MVOpt는 아직 커밋되지 않았습니다. 덮어쓰기 전에 MVOpt를 먼저 커밋해 두면 두 상태가 모두 이력에 남습니다.\"",
     "why": "The item is decided, and its one procedural instruction was followed by accident rather than by plan: MVOpt was committed at 3232d3e, so both states do exist in history — but only because the copy happened after that commit. Leaving §4 open invites someone to 'do' it a second time.",
     "fix": "Close §4 with the outcome and the commit hash; move it to investigations-closed.md."
    },
    {
     "severity": "minor",
     "what": "`docs/tools/{raw_table,per_frame_analysis,eval_validity,gt_cov}.py` all hardcode `SegMaskSam3OneStageNew` as the OLD-code arm of a OneStage-vs-OneStageNew delta — e.g. `docs/tools/per_frame_analysis.py:8` `M = ['SegMaskNew1', 'SegMaskSam3OneStage', 'SegMaskSam3OneStageNew']`, `:39` \"per-frame J delta OneStageNew-OneStage on view-index-0 cameras\", `docs/tools/raw_table.py:25`, `docs/tools/eval_validity.py:19`, `docs/tools/gt_cov.py:14`.",
     "why": "They read the stored mask directories, so they are correct today. They become wrong the moment anything regenerates `SegMaskSam3OneStageNew`, and they will not error — they will silently compare MVOpt-code output against OneStage and label it a OneStageNew delta.",
     "fix": "No code change needed now; note the dependency in the same provenance file so the coupling is visible."
    },
    {
     "severity": "minor",
     "what": "`figures/make_figure.py:2,14` and `figures/make_baseline_figure.py:15` point `SRC`/`OURS` at `/home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew` and read the numbered demo dirs `0`..`31`.",
     "why": "The figures still build — the 32 numbered directories survived intact (1,600 PNGs, 13 GB, dated 2026-03-11, root-owned, gitignored at `.gitignore:183`). But the docstring \"teaser figure for SCSam3 (demoSCSam3OneStageNew)\" now names a folder whose code is different from the code that produced those PNGs. Regenerating them by running that folder's `sam3_demoVideo.py` would use patched code (output-neutral per the S1-S7 evidence, so the images should not change).",
     "fix": "One-line comment noting the PNGs predate the 2026-09-04 code swap."
    },
    {
     "severity": "note",
     "what": "`docs/raw/critic.md:9` claims `demoSCSam3OneStageNew/.dockerignore` is \"**untracked in git** (`git status: ?? …/.dockerignore`)\" and is \"the one deleted file that is *not* recoverable\".",
     "why": "False, and it was false before the copy: `git ls-tree 3232d3e SCSam3/demoSCSam3OneStageNew/` lists `.dockerignore` and `SCSam3TrackerPredictorNewMem copy.py` among the 14 tracked files. Both survived the copy anyway, but the risk assessment that treated `.dockerignore` as unrecoverable was wrong on its own terms. (`docs/raw/env-docker.md:46` separately establishes the file has no effect: Docker only honours a `.dockerignore` at the build context root, which is `SCSam3/`.)",
     "fix": "Correct critic.md:9."
    },
    {
     "severity": "note",
     "what": "`docs/experiments.md:144` lists `| SegMaskSam3OneStageNew_verify | 진행중 | 검증 부산물 — MVOpt 동등성 재확인용 |`, and investigations-closed.md:200 records that rerun as 12,428 PNGs with Welder contributing 677.",
     "why": "The directories are now complete for 12 datasets and hold 12,472 PNGs, with Welder at 721 (vs. 941 in the stored baseline — still the truncated OOM run). The 44-PNG gap against the recorded 12,428/677 predates the copy and is not caused by it, but the counts in the doc no longer match disk.",
     "fix": "Refresh the count and change 진행중 → 12 (Welder 부분)."
    },
    {
     "severity": "note",
     "what": "`docs/raw/sweep_0.md`, `sweep_1.md` and `sweep_2.md` are pre-copy risk analyses of this exact action and remain accurate — e.g. sweep_2.md:35 \"**the name `OneStageNew` stops meaning \\\"the baseline code\\\"** with nothing in the tree marking the transition\", sweep_2.md:88 \"**Nothing on disk records which code wrote a mask dir** — no manifest, no commit hash, no run stamp\", sweep_1.md:54 \"after the copy **no directory on disk still contains OneStageNew code**, so the baseline becomes unreproducible\", sweep_0.md:77 \"`waitAndRunMVSeg.sh` will `--overwrite` the published `SegMaskSam3OneStageNew` baseline masks the next time it runs\".",
     "why": "These three files are the only correct account of the current state, and `docs/README.md` does not reference them by name (only `raw/` generically at :36), so they are effectively orphaned while every indexed doc says the opposite.",
     "fix": "Link them from docs/README.md and from the top of memory-optimization.md; they are the migration record."
    }
   ],
   "evidence": [
    "`git status --short` → only 6 modified files, all under `SCSam3/demoSCSam3OneStageNew/`; `git diff --stat` → 192 insertions, 44 deletions across SCSam3TrackerPredictor.py, SCSam3TrackerPredictorNewMem.py, SCSam3Video.py, SCSam3VideoInference.py, SCSam3VideoInferenceNewMem.py, io_utils.py",
    "md5 of all 12 shared .py files is identical between demoSCSam3MVOpt and demoSCSam3OneStageNew; `diff -r -q SCSam3/demoSCSam3OneStageNew SCSam3/demoSCSam3MVOpt` reports 0 differing files",
    "Marker census on disk — MVOpt: S1=3+3 sentinels, S2 leftover writes=0, S3 device=\"meta\"=2, S4 background_value==0 =4, S5 img.to(torch.float32)=1, S6 uses_spatial_predictor=1, S7 SCSAM3_TRIM_CACHED_OUTPUTS=2. OneStageNew: identical on all seven. `[MEM]` tag count 2 in both (0 before the copy per patch-matrix.md:72)",
    "`git show 3232d3e:SCSam3/demoSCSam3OneStageNew/SCSam3Video.py | grep RetireSpatialPredictor` → empty; `git show 3232d3e:SCSam3/demoSCSam3OneStageNew/SCSam3VideoInferenceNewMem.py | grep SCSAM3_TRIM_CACHED_OUTPUTS` → empty. Both present now at SCSam3Video.py:23,37 and SCSam3VideoInferenceNewMem.py:38,546",
    "md5 `0bcbaed127a9ce96c18b49e18ad319b6` (3232d3e OneStageNew SCSam3TrackerPredictor.py) matches none of the 7 SCSam3TrackerPredictor.py copies on disk; `/tmp/sibling_backup/` contains demoSCSam3ForSam2, ForSam2New, OneStage, TwoStage, TwoStageNew — no OneStageNew",
    "SCSam3/runMVSegAll.sh:25 `ALGOS=(OneStage OneStageNew)`; :26 `OUTNAME=([OneStage]=SegMaskSam3OneStage [OneStageNew]=SegMaskSam3OneStageNew)`; :58-61 `docker run ... python runMVSeg.py \"${ds}\" --algo \"${algo}\" \"${EXTRA[@]}\"` with no --memory cap",
    "SCSam3/waitAndRunMVSeg.sh:55 `./runMVSegAll.sh --algo OneStageNew --no-eval --overwrite Frog`; :63 `./runMVSegAll.sh --overwrite`",
    "SCSam3/runMVOptThree.sh:13-16 `docker run ... --memory=90g -e SCSAM3_TRIM_CACHED_OUTPUTS=1 ... python runMVSeg.py \"${ds}\" --algo \"${ALGO:-MVOpt}\" ${OUT:+--out \"${OUT}\"} --overwrite`",
    "SCSam3/runMVSeg.py:38-46 ALGOS/DEFAULT_OUT/NEEDS_ALL_VIEWS; :251-252 `if getattr(self, \"uses_spatial_predictor\", getattr(self, \"predictor_spatial\", None) is not None):`; :294-295 `if not args.overwrite and os.path.isdir(out_dir) and os.listdir(out_dir): sys.exit(...)`; :335-338 `if hasattr(sc, \"RetireSpatialPredictor\"): sc.RetireSpatialPredictor(); torch.clear_autocast_cache(); print(\"spatial model  retired\")`",
    "`ls -l --time-style=long-iso` — demoSCSam3OneStageNew/*.py now carry MVOpt's mtimes 2026-09-04 01:00/01:02; Data/MVSeg/Blocks/SegMaskSam3OneStageNew_verify deepest PNG is 2026-09-04 10:35 (log SCSam3/logs/onestagenew-verify-101913.log)",
    "Data/MVSeg inventory — SegMaskSam3OneStageNew present for 12 datasets (12,692 PNGs, dir mtime 2026-09-03 09:38 for Blocks), SegMaskSam3OneStageNew_verify for 12 (12,472 PNGs; Welder 721 vs stored 941), SegMaskSam3MVOpt for 15, SegMaskSam3OneStage for 15; `diff -rq Blocks/SegMaskSam3OneStageNew Blocks/SegMaskSam3OneStageNew_verify` → identical",
    "Data/MVSeg jf files and mtimes: jf_sam3_onestagenew.json 2026-09-03 13:19, jf_sam3_onestagenew_full.json 2026-09-04 01:47, jf_sam3_mvopt_all.json 2026-09-04 06:49; `Data/` is gitignored at .gitignore:172, PNGs at .gitignore:183 — no git history for any of it",
    "`git ls-tree 3232d3e SCSam3/demoSCSam3OneStageNew/` lists 14 tracked files including `.dockerignore` and `SCSam3TrackerPredictorNewMem copy.py`; that backup differs from the 3232d3e live file by exactly one line (`s_pos_and_prevs.append((s_pos, None))` at :1257), so it is a pre-copy-era artefact and was not touched",
    "32 numbered directories (0..31) survived: 1,600 PNGs, 13 GB total, 4.4 MB each, dated 2026-03-11, root-owned, matched by .gitignore:183"
   ]
  },
  {
   "axis": "git-recoverability",
   "ok": false,
   "confidence": "high",
   "findings": [
    {
     "severity": "blocker",
     "what": "Commit 3232d3e is NOT pushed. `git rev-parse main origin/main` gives 3232d3e4f2d7... vs 99c05c746f2e...; `git merge-base --is-ancestor 3232d3e origin/main` fails; `git log --oneline origin/main..main` lists exactly one unpushed commit (3232d3e). `git ls-remote origin` confirms the remote HEAD and refs/heads/main are both at 99c05c7. So the answer to (e) is: 3232d3e exists ONLY on this machine. It contains 89 files (70 added, 19 modified), including the ENTIRE SCSam3/demoSCSam3MVOpt package (the stated future-development target) and the ENTIRE docs/ tree (the memory-optimization documentation).",
     "why": "A disk failure or an accidental `git reset --hard 99c05c7` loses all seven memory optimizations, the MVOpt package, and every analysis document. The 'frozen snapshot / it's all in git' safety story does not hold off-machine for anything except the six .py blobs (see mitigation below). Nothing marks the baseline either: `git tag -l` is empty, and docs/raw/sweep_2.md:99 already recommended `git tag -a baseline-onestagenew-jf 99c05c7` — never done.",
     "fix": "Run `git push origin main` now, and `git tag -a baseline-onestagenew-jf 99c05c7 -m \"code behind jf_sam3_onestagenew.json\" && git push origin baseline-onestagenew-jf`. Mitigation that already exists: the six .py originals are byte-identical between 99c05c7 and 3232d3e (verified blob-for-blob), and 99c05c7 IS on the remote, so the pre-copy CODE itself is recoverable from GitHub via `git show 99c05c7:<path>`."
    },
    {
     "severity": "important",
     "what": "The 32 numbered output directories are not in git at any commit and are unrecoverable from it. `git ls-tree -r 3232d3e -- SCSam3/demoSCSam3OneStageNew/ | grep -cE '/[0-9]+/'` returns 0. They are ignored by `.gitignore:183` (`*.png`), confirmed by `git check-ignore -v SCSam3/demoSCSam3OneStageNew/0/11.png` -> `.gitignore:183:*.png`. Measured: 13 GB total, 1600 PNGs.",
     "why": "They survived this operation only because the copy was file-level (six `cp` of .py files). Any future `rsync -a --delete`, `rm -rf` + `cp -r`, or directory-level sync from demoSCSam3MVOpt (which has no numbered dirs) destroys 13 GB permanently with no git baseline. Note the discrepancy with the task brief's '12,428 fresh PNGs': these 32 dirs hold 1600 PNGs and are demo output written by sam3_demoVideo.py, not the MVSeg equivalence-run outputs.",
     "fix": "Never use a directory-level copy between the two folders. If the numbered dirs matter as evidence, snapshot them outside the repo; if they are regenerable demo output, say so explicitly in docs so a future user does not treat them as precious."
    },
    {
     "severity": "important",
     "what": "SCSam3/demoSCSam3OneStageNew/.dockerignore is tracked at 3232d3e (blob cf81960f6550cf447e4ec71db156daff2b0bbbf5, content `*` / `!*.py`) and is unmodified in the worktree — but `git show 99c05c7:SCSam3/demoSCSam3OneStageNew/.dockerignore` returns `fatal: ... exists on disk, but not in '99c05c7'`. It was added by the unpushed commit.",
     "why": "It is recoverable locally but has no off-machine copy. This also means the warning in docs/raw/critic.md:9 ('untracked in git ... the one deleted file that is not recoverable') is now STALE — it was fixed by 3232d3e. A future reader following that doc would wrongly believe it is still untracked.",
     "fix": "Push main (which fixes the off-machine gap), and correct or date-stamp the stale claim in docs/raw/critic.md:9."
    },
    {
     "severity": "note",
     "what": "Verification detail worth recording: all six copied files are byte-identical to their demoSCSam3MVOpt counterparts (git hash-object comparison: 6/6 IDENTICAL), and all six differ from their 3232d3e originals (6/6 DIFFERS). The other seven tracked .py files in the folder are untouched.",
     "why": "Confirms the stated intent — demoSCSam3OneStageNew is now an exact mirror of demoSCSam3MVOpt for those six files, with no partial or corrupted copy.",
     "fix": "None needed. Re-run the hash comparison after any future sync to keep the 'byte-identical from now on' invariant checkable."
    }
   ],
   "evidence": [
    "(a) CONFIRMED — all six originals present at 3232d3e. `git cat-file -e 3232d3e:<path>` succeeded for all six, blobs: SCSam3TrackerPredictor.py=086e4e19, SCSam3TrackerPredictorNewMem.py=cb9d7f0d, SCSam3Video.py=a70420a0, SCSam3VideoInference.py=3a4362e7, SCSam3VideoInferenceNewMem.py=6bee144c, io_utils.py=bcac6da5.",
    "(a) EXTRACTION PROOF — `git show 3232d3e:SCSam3/demoSCSam3OneStageNew/SCSam3VideoInference.py` wrote 92,824 bytes; worktree file is 92,948 bytes. `diff -u` shows the original code (`if \"previous_stages_out\" not in inference_state: ...` / `inference_state[\"mask_inputs_per_obj\"][obj_id][frame_idx] = mask`) replaced by the S1 sentinel `= \"_THIS_FRAME_HAS_OUTPUTS_\"` and the S2 removal. Extracted blob hash 3a4362e7... matches the 3232d3e blob exactly.",
    "(a) Per-file hash matrix: all six report orig-vs-worktree=DIFFERS and worktree-vs-MVOpt=IDENTICAL. `git diff --stat` = 6 files changed, 192 insertions(+), 44 deletions(-).",
    "(b) `git ls-tree -r --name-only 3232d3e -- SCSam3/demoSCSam3OneStageNew/` lists 14 entries: .dockerignore plus 13 .py — SCSam3TrackerPredictor.py, 'SCSam3TrackerPredictorNewMem copy.py', SCSam3TrackerPredictorNewMem.py, SCSam3Video.py, SCSam3VideoInference.py, SCSam3VideoInferenceNewMem.py, SCSam3VideoPredictor.py, SCSam3VideoPredictorNewMem.py, build_scsam3.py, io_utils.py, misc.py, sam3_demoVideo.py, test.py. `find -maxdepth 1 -name '*.py'` on disk returns the same 13. NONE LOST — the backup 'SCSam3TrackerPredictorNewMem copy.py' (97,418 B) is tracked, and the seven non-copied .py files are unmodified (git status lists only the six).",
    "(c) RESTORE COMMAND — `git checkout 3232d3e -- SCSam3/demoSCSam3OneStageNew/`. TESTED, not merely asserted: created an isolated `git worktree add --detach <scratch>/wt 3232d3e`, overwrote SCSam3VideoInference.py there with the current MVOpt version (status showed ' M'), ran the command (exit 0), and `git status --porcelain` came back empty = fully restored. The whole-directory pathspec also handles the space in 'SCSam3TrackerPredictorNewMem copy.py' without quoting. The test worktree was removed; `git worktree list` now shows only the main tree.",
    "(d) NUMBERED DIRS — 0 tracked at 3232d3e; `git status --ignored` reports `!! SCSam3/demoSCSam3OneStageNew/0/`; `git check-ignore -v SCSam3/demoSCSam3OneStageNew/0/11.png` -> `.gitignore:183:*.png`. `du -ch` = 13G total, `find -name '*.png' | wc -l` = 1600. NOT recoverable from git.",
    "(d) .dockerignore — tracked at 3232d3e (blob cf81960f), `git diff --stat HEAD` empty (unmodified), but absent from 99c05c7.",
    "(d) SCSam3/sam3/ — `git check-ignore -v` -> `.gitignore:185:SCSam3/sam3/`, with the comment on the preceding line 'upstream SAM 3 체크아웃 (자체 .git 보유, 별도 저장소)'. `ls -ld SCSam3/sam3/.git` confirms a nested repo; its remote is github.com/facebookresearch/sam3.git, HEAD f6e51f5, `git status -sb` = '## main...origin/main' (no ahead/behind), `git log --not --remotes` EMPTY (zero local-only commits), working tree clean. Gitignoring it is CORRECT and it is fully recoverable by re-clone.",
    "(e) CLEAN — `git status --porcelain` prints exactly six ' M' lines (the six copied files) and nothing else; `git diff --cached --stat` is empty (nothing staged); `git status --porcelain --ignored=traditional` summarizes as 251 '!!' + 6 'M' with zero '??' untracked entries.",
    "(e) REMOTE — `git remote -v` shows origin https://github.com/sjpark0/SCSegmentation.git (fetch/push). BUT `git ls-remote origin` -> HEAD and refs/heads/main both 99c05c746f2ec1ce983ffe32f8ad2687b854f4eb, `git branch -r --contains 3232d3e` returns nothing, and `git log --oneline origin/main..main` lists 3232d3e as unpushed. `git diff --name-only 99c05c7 3232d3e | wc -l` = 89 files local-only.",
    "(e) MITIGATION VERIFIED — for all six files, `git rev-parse 99c05c7:<path>` equals `git rev-parse 3232d3e:<path>` (SAME blob, 6/6). `git diff --stat 99c05c7 3232d3e -- SCSam3/demoSCSam3OneStageNew/` shows only `.dockerignore | 2 ++`. So the pre-copy .py sources DO exist off this machine, at the pushed commit 99c05c7."
   ]
  }
 ],
 "critic": "## 1. The risk class none of the five looked at: **live process / runtime state**\n\nAll five reasoned about files at rest. Nobody ran `ps`. **A job is executing against the copied files right now.**\n\n```\n1196056 sjpark 11:11:49  bash -c ... ALGO=OneStageNew OUT=SegMaskSam3OneStageNew_verify ./runMVOptThree.sh Welder AlexaMeadeExhibit CoffeeMartini FlameSteak\n1208433 sjpark 11:19:58  docker run --rm --gpus all --memory=90g -e SCSAM3_TRIM_CACHED_OUTPUTS=1 \\\n                           -v /:/host ... scsam3 python runMVSeg.py AlexaMeadeExhibit --algo OneStageNew \\\n                           --out SegMaskSam3OneStageNew_verify --overwrite\n1208515 root   11:19:59  python runMVSeg.py AlexaMeadeExhibit --algo OneStageNew ...\n```\n\nTiming, from `stat -c '%z'`: the six files' **ctime is `2026-09-04 11:11:21`** (mtime is MVOpt's preserved `01:00`/`01:02`). The job launched at `11:11:49` — **28 seconds after the copy**. So the copy was not mid-run, but everything since is running patched code under the name OneStageNew.\n\n**(a) The running job already destroyed the only physical evidence of real OneStageNew behavior.**\n\n`SCSam3/logs/onestagenew-verify-101913.log` (pre-copy run, 10:19–11:05) ends:\n\n```\nWelder               FAILED(137)   388s\nDIFFER  Welder                   677 PNG  차이 18건\nTOTAL 12428 PNG, 차이 18건\n!!! 차이 있음 !!!\n```\n\n`docs/investigations-closed.md:200` records that same run as `| **OneStageNew 신규 재실행 vs MVOpt 저장본** (2026-09-04) | PNG 12,428장 | **0** |`. The count matches; the verdict does not. The log says 18, the doc says 0. Welder was SIGKILLed (137) after 677 of 941 PNGs, so the 18 are plausibly missing-path lines rather than content differences — but the artifact that would settle it is gone:\n\n```\n/Data/MVSeg/Welder/SegMaskSam3OneStageNew_verify  total=941 pre-copy=0 post-copy=941\n```\n\nEvery one of the 941 PNGs now has mtime > 11:11. The 677-file pre-copy output was overwritten by the running job at ~11:19. `runMVSeg.py` has no `rmtree` — only `os.makedirs(..., exist_ok=True)` at `runMVSeg.py:350` — so `--overwrite` overwrites in place and leaves stragglers, which is how a 677-file dir became a 941-file dir with no trace of the earlier run.\n\n**(b) The running job is a tautology.** `args.algo` is used at only five places: `runMVSeg.py:116` (`algo_dir = ALGOS[algo]`), `:265` (out name), `:284` (print), `:300`, `:304` (`NEEDS_ALL_VIEWS`, which contains both `OneStageNew` and `MVOpt` per `:45`). With all 12 shared `.py` byte-identical, `--algo OneStageNew` and `--algo MVOpt` execute the same bytes. It is burning ~90 GB RAM and a GPU to re-test GPU determinism, not code equivalence — and it cannot answer \"does the frozen baseline still reproduce,\" because no OneStageNew code exists on disk to ask.\n\n**(c) Ownership.** The container process runs as **root** (PID 1208515). Live proof: `AlexaMeadeExhibit/SegMaskSam3OneStageNew_verify` is `root:root`. `find /home/sjpark/Documents/SCSegmentation -xdev \\! -user sjpark | wc -l` → **10514**. The chowns at `runMVOptThree.sh:23` and `runMVSegAll.sh:73-74` cover **only `Data/MVSeg`** and run only if the loop completes — a Ctrl-C leaves root-owned output. Uncovered, and confirmed root-owned:\n\n```\ndrwxr-xr-x 2 root root 4096 Mar  9 12:30 SCSam3/demoSCSam3OneStageNew/0\ndrwxr-xr-x 2 root root 4096 Mar  9 12:31 SCSam3/demoSCSam3OneStageNew/31\ndrwxr-xr-x 2 root root 4096 Sep  3 19:33 SCSam3/__pycache__\n```\n\nThe 13 GB the task says \"had to survive\" is root-owned. That is accidental protection from `sjpark`, and zero protection from any `docker run -v /:/host` — which is every runner in this repo.\n\n**(d) `__pycache__` — I checked, and it is clean.** All 13 `.pyc` in `demoSCSam3OneStageNew/__pycache__` carry `flags=0` (timestamp invalidation), magic `cb0d0d0a` matching CPython 3.12, and header `(mtime,size)` exactly equal to their source's — `VALID=True` for all 13, including the six copied files (e.g. `SCSam3VideoInferenceNewMem.py pyc(mt=1788451325,sz=99150) src(mt=1788451325,sz=99150)`). No shadowing. Worth knowing *why* it is safe: CPython compares source mtime for **equality**, not \"newer than,\" so `cp -p` moving a destination's mtime backward does not fool it. Tools that do use newer-than (make, `rsync -u`, editor external-change detection) will see these six files as 10 hours old.\n\nOne process-hygiene note the code-equivalence verifier reported but did not classify as a risk: its `py_compile` ran inside a container **at 11:14/11:16, while the Welder job was live** (`Welder.log` mtime 11:19). Writing into a running job's package directory. It was safe here only because `.pyc` writes are atomic renames and Welder had already imported.\n\n**(e) Silent drift** — see Q2.\n\n## 2. Mechanism keeping the freeze true: **there is none**\n\nEverything I checked came back empty:\n\n- `ls -la .git/hooks/ | grep -v '\\.sample'` → only `.` and `..`. No active hooks.\n- `find .github -type f` → `copilot-instructions.md` only. No CI.\n- `.pre-commit-config.yaml` → `No such file or directory`.\n- `grep -rl 'demoSCSam3OneStageNew' --include='*.sh' --include='*.yml' --include='Makefile' --include='*.toml'` → **no matches**. Not one script, config, or Makefile mentions the folder.\n- No manifest, hash file, `FROZEN` marker, or README stub inside `demoSCSam3OneStageNew/`.\n\nThe only thing that mentions it is `runMVSeg.py:38-40`, which maps `\"OneStageNew\": \"demoSCSam3OneStageNew\"` — i.e. the folder is a **live execution target**, not a backup.\n\nHow the freeze fails silently:\n\n1. **No image pin.** `SCSam3/Dockerfile` `COPY ./sam3 /opt/sam3` and nothing else; the demo packages arrive purely through `-v /:/host`. Whatever is on disk at `docker run` time is what runs. There is no baked artifact to fall back to. (`demoSCSam3OneStageNew/.dockerignore` is inert — Docker honors only the context-root `.dockerignore`, and the context is `SCSam3/`.)\n2. **`--algo OneStageNew` still resolves and still runs.** Nothing errors, warns, or refuses. The currently-running job is the proof.\n3. **The freeze is unenforceable in the other direction too.** Any edit to MVOpt silently makes the two folders diverge, and the only signal is a `diff` nobody is scheduled to run.\n4. **Outputs are invisible to git** (`.gitignore:183` `*.png`), so no `git status` will ever show that a mask directory was rewritten. `--overwrite` plus no `rmtree` means a rewrite is also invisible on disk except by mtime.\n5. **`3232d3e` is unpushed** — `git reflog` confirms it is `HEAD@{0}`, one commit ahead of `origin/main` at `99c05c7`. It is the only surviving copy of real OneStageNew source. I verified it is genuinely the pre-patch code: `git show 3232d3e:SCSam3/demoSCSam3OneStageNew/SCSam3Video.py | grep -c 'uses_spatial_predictor\\|RetireSpatialPredictor'` → **0**. One `.git` loss and the baseline source is gone permanently, since the PNGs it produced were never tracked either.\n\n## 3. Cheapest checks that materially raise confidence\n\n**First — stop the running job.** It is a tautology that is actively overwriting `SegMaskSam3OneStageNew_verify` and writing root-owned files:\n\n```\nkill 1196056 1196057 && docker stop d86a0c9b2aeb\n```\n\n**Then, the single highest-value command — push the only copy of the baseline (seconds, no GPU):**\n\n```\ngit -C /home/sjpark/Documents/SCSegmentation push origin main\n```\n\n**Then the drift check.** This is the whole freeze contract in one line; it exits 0 today (I ran it):\n\n```\ndiff -rq --exclude=__pycache__ -x '*copy.py' -x '.dockerignore' -x '[0-9]' -x '[0-9][0-9]' \\\n  /home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew \\\n  /home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3MVOpt\n```\n\nAll 12 shared `.py` share sha256 (e.g. `SCSam3Video.py` → `9db2a57cfd58215cc954b1f93a058c6fe40b72ddf075de74d82f96b9855ee7bc` in both). Wire it into `.git/hooks/pre-commit`; it is the only thing that will ever notice drift.\n\n**Finally, make the 13 GB checkable** — it is git-invisible, root-owned, and has no integrity record, so silent corruption is currently undetectable:\n\n```\ncd /home/sjpark/Documents/SCSegmentation/SCSam3/demoSCSam3OneStageNew && \\\n  find [0-9] [0-9][0-9] -name '*.png' | sort | xargs sha256sum > MANIFEST.sha256\n```\n\n(~1600 files; under a minute. Commit `MANIFEST.sha256` — it is not matched by `.gitignore:183`.)\n\nOne documentation fix that costs nothing: `docs/investigations-closed.md:200` says **0** differences for a run whose own log (`SCSam3/logs/onestagenew-verify-101913.log`) says **차이 18건** and `!!! 차이 있음 !!!`. Correct the line or record why the 18 are benign — the output that would prove it is already overwritten."
}