# S2-R2 결과 — 크롭 추적을 본류에 구현해 헤드라인 구성으로 실행: basic 91.0 / complete 86.4

> 2026-09-10. 사전 등록 [stage2-R2-prereg.md](stage2-R2-prereg.md)(2cdba1e) → 구현 ee75662(테스트 405→436) → 17장면 실행
> (`SCSam3/logs/cs10k-*`, 장면당 85~795초, 합계 약 80분; CoffeeMartini는 하네스 래퍼가 죽은 뒤에도 컨테이너가 완주,
> FlameSteak 이후 6장면은 분리 드라이버로 실행) → `eval_jf` → `report_jf --muvod`. 출력 폴더 `SegMaskSam3XW1CGPS4MFsCs10kG2`,
> 채점 `Data/MVSeg/jf_r2.json`. 구성: `--xview-window 1 --xview-mode C --xview-gate --xview-ptr --xview-tpos-shift 4 --ref-cam muvod
> --frame0-seed --crop-small 10000 --crop-gate v2`.

## 1. 결과 (MUVOD 17장면, J&F³ %)

| 구성 | basic | complete | vs XMem |
|---|---|---|---|
| MUVOD XMem 기준선 | 79.4 | 75.6 | — |
| `SegMaskSam3XW0M` (대조군, 이웃 읽기 없음) | 89.5 | 85.0 | +10.2 |
| `SegMaskSam3XW1CGPS4M` (이전 헤드라인) | 90.034 | 85.5 | +10.7 |
| **`SegMaskSam3XW1CGPS4MFsCs10kG2` (새 헤드라인)** | **91.015** | **86.4** | **+11.7 / +10.8** |

이전 헤드라인 대비 basic **Δ +0.981점, 클러스터 CI [+0.446, +1.613], 16↑ / 0↓**(Frog 0.00). 장면별: AlexaMeadeExhibit 85.9→90.0, Blocks 88.2→91.3, Breakfast 85.0→87.8, MATF 83.4→85.4, Barn +1.1, CoffeeMartini +0.8, FlameSteak +0.7, PoznanStreet +0.6, Fencing +0.3, Carpark +0.3, Welder 84.2→84.5(XMem 85.2에 −0.7), 나머지 0~+0.2.

## 2. 구현이 R1의 정의와 일치하는가 (예측 b)

| 검사 | 결과 |
|---|---|
| 대상·되돌림 집합 = R1 조립(Cr10k+v2) | 17/17 장면 동일(349 대상 / 71 되돌림) |
| 비대상 (카메라, 객체)의 프레임 1~20 PNG = 이전 헤드라인 | **16,051 / 16,051 바이트 동일** |
| 채택된 대상 쌍의 프레임 1~20 PNG = 조립 폴더 | **5,254 / 5,254 바이트 동일** |
| 점수 = 조립 추정(91.015 / 86.4) | 동일 |

즉 "크롭 결과는 이웃 기억으로 흐르지 않고, 크롭 세션은 이웃을 읽지 않는다"는 정의대로 구현됐고, 본류 실행이 결정적임도 다시 확인됐습니다(이전 헤드라인 PNG 전부 재현).

## 3. 예측 대조

| # | 예측 | 결과 | |
|---|---|---|---|
| a | basic 91.0 ± 0.15, complete 86.4 ± 0.15 | 91.015 / 86.4 | ✔ |
| b | 비대상 바이트 동일; 대상/되돌림 349/71 | 16,051/16,051; 349/71 | ✔ |
| c | Δ CI 하한 > 0, 하락 장면 0 | [+0.446, +1.613]; 0 | ✔ |

## 4. 판정

채택 조건(Δ ≥ +0.5, 장면 < −0.3 없음, 비대상 바이트 동일) 전부 통과 → **헤드라인 = `SegMaskSam3XW1CGPS4MFsCs10kG2`, MUVOD basic 91.0 / complete 86.4.**

우위 분해(basic, XMem 79.4 기준): 기초 모델·2단계 구조 **+10.2** → 추적 중 이웃 읽기 **+0.5** → 프레임 0 시드 직기록 **+0.03** → **작은 객체 크롭 추적 +0.98** = **+11.7**.

남는 것: 게이트 규칙은 같은 17장면을 보고 정한 것(R1′·R1″)이라 논문에는 "게이트 규칙은 개발 데이터에서 정했다"를 적습니다; 줌<1.2 구간의 이득(Fencing)은 미회수; 이웃 기억과 크롭의 통합 변형(크롭 결과를 이웃에 흘림)은 별도 등록 대상. 사용자 결정대로 **1단계로 복귀**합니다.

## 5. 이력

| 일자 | 내용 |
|---|---|
| 2026-09-10 | 실행·판정. 등록 대비 규칙 변경 없음 |
